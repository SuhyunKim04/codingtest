let arr = [1,2,3,4,5, 1]
let set = new Set(arr)

console.log(set.size < arr.length ? 'double' : 'no double');
console.log(set, set.size)

let str = 'Hello School';
let strSet = new Set(str);

console.log(strSet)

